# T07 验收复核

- 实现提交：`1ff0679`
- 工程与安全结论：**APPROVED**，无 P0/P1/P2。
- T07 总验收结论：**PENDING**。

已通过：统一模型契约、严格输出解码、逐字段人工采纳事务、取消/超时/失败降级、附件边界、正式环境 DeepSeek 禁用、本地回环限制、数据库迁移和 20 条冻结评测契约。

待通过：

1. 用有效的开发测试密钥完整运行 20 条 DeepSeek 样本，保留原始输出哈希、模型版本、token 和时延证据，再由每条至少两名人工复核者标注。
2. 确定正式本地模型与目标硬件，在同一冻结样本上证明质量门槛和 p95 时延。
3. 用户明确允许恢复 Ego Browser 控制后，完成“勾选字段 → 采纳 → 仅选中字段变更”的真实页面证据。

在上述证据齐全前，T07 保持 `PENDING`，不用 Fake Adapter 或接口 200 代替模型质量验收。
