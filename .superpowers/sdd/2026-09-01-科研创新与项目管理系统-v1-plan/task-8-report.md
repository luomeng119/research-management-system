# T08 立项原子事务报告

- 立项决定、`project_registry`、唯一类别项目、提案状态和审计在同一 PostgreSQL 事务中提交；任一写入失败全部回滚。
- 幂等请求保存指纹和首次结果快照；同键同请求只生成一个项目，同键不同请求返回 409。
- 三类旧 URL 保留并委托统一 Service。项目业务编号全局唯一，继续兼容原附件目录、设备组和文档关联；不合并三张项目表，不复制三套新 API。
- 历史项目可以 `proposal_id=NULL`；迁移前检查 registry 双向完整性和跨类别重号，有冲突即阻断并保持原数据。
- 项目文档三类页面已使用各自 Blueprint URL、CSRF 和统一路径边界；目录初始化失败只降级提示，不回滚已创建的业务项目。

实现提交：`825bbb0`。
