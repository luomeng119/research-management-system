# T09 项目全周期与只读研究路径验收

- 完成项目启动、进展、风险/受阻、变更、成果、结题与终止的受控留痕；旧三类项目、文件和列表入口保留。
- 研究路径只读取固定阶段与真实进展记录；本地 G6 失败或路径 API 失败不影响项目详情，未引入任务、依赖、审批或可编辑思维导图。
- 状态、分类表、版本、变更记录和审计在同一持锁事务提交；过程列表支持分页，详情首屏 50 条并可加载更早记录。
- 真实 PostgreSQL：`0001 → 0004`、迁移往返、降级保护、并发一致性、11 项生命周期与 44 项数据库契约通过。
- 最终回归：`209 passed, 37 skipped, 1 deselected`；Playwright `5/5 passed`；代码、数据库、安全复核均 **APPROVED**，P0/P1/P2 为 0。

实现提交：`67973ef`。T09 工程验收：**APPROVED**；不代表真人 UAT 或最终部署环境验收。
