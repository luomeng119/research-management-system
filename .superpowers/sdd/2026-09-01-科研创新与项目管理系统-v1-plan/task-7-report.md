# T07 科研提案助手报告

## 产品边界

- 业务系统始终本地部署；V1 提案助手只作为可选的在线 DeepSeek 增强，不配置本地提案模型。
- 未配置 Key、断网、超时或 API 异常时，只禁用 AI；手工提案、立项及全部非 AI 业务继续运行，`/healthz` 不依赖 AI。
- 只有用户显式发起并确认本次文字和勾选附件允许外发、已完成必要脱敏时才调用 DeepSeek。
- AI 只返回临时结构化草稿；用户勾选字段后才能事务性采纳。不自动立项，不引入向量库、RAG、Agent、附件自动解析或自动重试。

## 实现与证据

- 严格 JSON 契约、输入/输出长度、禁止额外字段、取消/超时/错误不落草稿、版本冲突和采纳审计均已有自动化覆盖。
- 确定性终整只检查用户显式文字框，不扫描附件正文；否定句、引用指令和提示注入反例不会被转成研究目标。
- Ego Browser 已验证“生成建议 → 只勾选名称和目标 → 采纳 → 刷新持久化”；只有勾选字段变化，版本 `1→2`。
- `task-7-deepseek-eval-v13-raw.json` 是当前最终代码上的 20 条真实 DeepSeek V4 Flash 原始运行证据；`task-7-deepseek-eval-v13-reviewed.json` 只在同一批冻结记录上追加双代理评审注释。两份均分开保存 provider 原输出、原输出哈希、finalized 输出和 finalized 哈希。自动指标：schema `20/20`、禁止项 `20/20`、p95 `3.421s`、最大 `3.636s`、token 与两层输出哈希均通过。
- “张老师”和“李老师”为两名独立 AI 代理的模拟评审别名，不是真人签字。按更严格的李老师结果记录：严重幻觉 `0`、事实准确率 `51/52`、字段覆盖 `21/22`、缺失信息召回 `46/50`，结果 `PASSED`。
- 评测证据中不包含 API Key、Authorization 或 Bearer 值。
- 原始证据 SHA256：`91bd7906a262a64a398c42b2f44b7a53b8b49692804bb70e512fd999edbdb095`；追加评审后证据 SHA256：`63e0d9f4319810fe440fb69a73fb2bed9d0b517b42eafaf471d4fafabbff4dc8`。

## 当前状态

T07 工程验收完成：全量回归 `194 passed, 34 skipped, 1 deselected`，编译检查、diff 检查、代码复审和安全复审通过，无 P0/P1/P2。该结论不等于真人 UAT。
