# 科研管理系统去本地模型与源码交付：现状审计

- 审计日期：2026-09-21
- 源码基线：`57caa3fa8d6a9eff76c91748e2878f9e4ed3d07b`，工作树包含既有未提交产品改动
- 当前分支：`stage09/v1-development`
- 审计范围：模型资产、AI 功能入口、人工科研报告链路、仓库安全边界、交付文档

## 代码与运行基线

1. `config.py` 默认 `AI_PROVIDER=DISABLED`、`ENABLE_LLM=False`，但仍保存 `LOCAL_MODEL_BASE_URL`、`LOCAL_MODEL_NAME` 和控制器配置。
2. `app/__init__.py` 已在部分服务初始化处判断 provider，但没有统一的产品级 AI 可见性开关。
3. `app/routes/utils.py` 的模型维护路由只判断 provider 或旧开关；其他报告生成、图片提取、选区建议入口仍需统一门禁。
4. `app/templates/leadership/system_intro.html`、登录页、辅助工具页和科研报告模板仍显示本地模型、Qwen3.5-9B 或 AI 动作。
5. 人工科研报告、版本、敏感词规则和 DOCX 导出具有独立服务与数据结构，应保留。
6. 本项目本地生成模型端口 `18081` 当前无监听；`18080` 的 BGE-M3 属于其他用途，不在本次范围。

## 模型资产基线

- 项目专属模型目录：`/Users/lm/Documents/Codex/tasks/20260910-科研管理系统复盘与本地模型调整/models`
- 删除前统计：64 个文件，逻辑大小 22,483,221,919 字节（约 20.94 GiB）。
- 内容包括 Qwen3.5-9B、4B、多模态投影文件、14B 未完成分片和重复下载分片。
- 删除前清单：`/Users/lm/Documents/Codex/tasks/20260921-科研管理系统移除本地模型与私有仓库交付/evidence/model-assets-before.json`

## 仓库与安全基线

1. 当前仓库未配置远端，拟用新仓库名 `luomeng119/research-management-system`。
2. 现有 `.gitignore` 尚未覆盖模型文件、环境密钥、上传目录、运行证据等全部发布排除项。
3. `data/` 包含日志、会话或本地数据，必须排除；源码与测试夹具需逐项区分。
4. 当前工作树变更量大，必须保留既有功能，禁止用清理动作重置工作树。
5. 现有 `luomeng119/ai-archives-research-platform` 是另一项目，不得覆盖。

## AI 基线结论

本轮不评估模型质量，也不替换模型。目标是将本地生成模型从当前交付运行面移除：删除项目模型资产，默认不初始化 AI 服务，不展示 AI 入口，并保留未来重新启用所需的源代码边界。人工报告、版本、敏感词和 DOCX 为当前交付业务能力。

## 高风险边界

- 删除范围错误可能影响其他项目的 BGE-M3，必须使用精确目录并在删除后复核保护文件。
- 仅隐藏前端而未关闭后端会留下可直接访问的 AI 路由，必须同时执行配置、服务初始化、路由和模板四层门禁。
- 交付文档中的旧本地模型描述如直接入库会与当前版本冲突，必须标注历史状态或更新当前说明。
- GitHub 个人私有仓库不能实现匿名访问；仓库可见性与协作者策略必须按平台能力验证。
