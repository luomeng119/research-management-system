# 领导介绍页与登录页回滚说明

> **历史状态（已被取代）：**本文件只描述 2026-09-11 展示层变更的回滚方式；其中关于保留本地模型进程的说明不适用于 2026-09-21 当前交付。当前边界见[2026-09-21 无本地生成模型交付说明](2026-09-21-no-local-model-handoff.md)。

## 回滚范围

本次没有数据库迁移、模型变更或业务数据写入。回滚只涉及展示层：

1. 删除 `app/routes/leadership.py`、介绍页模板、专用 CSS/JS 和对应测试。
2. 从 `app/__init__.py` 的路由注册和 `PUBLIC_ENDPOINTS` 中移除 `leadership`。
3. 将 `app/templates/login.html` 恢复为本次改造前版本。
4. 只重启 8893 应用，不重启 PostgreSQL 和本地模型。

## 回滚可行性核对

- 新增路由没有数据库依赖，移除后不会留下数据残项。
- 所有介绍图为 HTML/CSS，不存在外部图片或资源清理问题。
- 登录 POST 处理器未修改；恢复模板即可恢复原界面。
- 新增公开端点是单独的精确端点名，不扩大其他页面公开范围。

## 回滚后检查

- `/system-intro` 返回 404 或跳转登录。
- `/auth/login` 仍返回 200，`admin/admin` 可登录。
- `/health/ready` 返回 200。
- 匿名访问业务页面仍跳转 `/auth/login`。
