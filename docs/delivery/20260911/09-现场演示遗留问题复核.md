# 科研管理系统现场演示前遗留问题复核

复核时间：2026-09-11 14:30（Asia/Shanghai）

结论：当前系统满足本机现场演示条件；正式源码交付尚未冻结。

| 编号 | 核对项 | 状态 | 当前结果 | 遗留问题或下一步 |
| --- | --- | --- | --- | --- |
| R-01 | 固定端口与服务健康 | ✅ 完成 | 应用 `8893`、模型 `18081`、数据库 `58562` 均正常 | 保持现有端口 |
| R-02 | 登录入口与管理员 | ✅ 完成 | 登录首界面可达，`admin` 可进入工作台，权限边界已有验收 | 无 |
| R-03 | 领导介绍页 | ✅ 完成 | 七项目录均可切换；固定 `8893` 端口 Playwright 复核通过 | 无 |
| R-04 | 数据流程 | ✅ 完成 | 资料来源、入库关联、插入/编辑/合并/删除、版本与输出已分四类说明 | 无 |
| R-05 | 当前生成模型 | ✅ 完成 | 仅加载 Qwen3.5-9B Q8_0；接口只返回 9B；2B 文件为 0 | BGE-M3 是独立向量嵌入服务，不属于 2B/9B 生成模型切换 |
| R-06 | 模型加载与推理 | ✅ 完成 | 模型状态正常；现场最小文本推理 0.57 秒返回 | 无 |
| R-07 | AI 入口与边界 | ✅ 完成 | 提案助手、图片提取、资料整合草稿、框选措辞建议均有 AI 标识 | 词库、版本、审计继续由确定性程序负责 |
| R-08 | 真实多模态图文报告 | ✅ 完成 | 两张图片和一份文本整合为草稿，人工确认后形成 v2 并导出含图 DOCX | 无 |
| R-09 | 敏感词库维护 | ✅ 完成 | 当前 v3；逐条编辑、启停、JSON 导入、预览、保存、历史和添加规则均可用 | 删除采用禁用并保留历史 |
| R-10 | 自动替换与框选建议 | ✅ 完成 | 报告自动确定性替换；框选内容可获取候选并一键替换 | 无 |
| R-11 | 文档完整流程 | ✅ 完成 | 上传、关联、编辑、合并、版本、下载均有 V1 用例和证据 | 无 |
| R-12 | Word 报告 | ✅ 完成 | DOCX 可打开；正文、版本、来源、代称和清洁图片正确；四页渲染通过 | 无 |
| R-13 | 当前完整 Python 回归 | ✅ 完成 | `1237 passed, 179 skipped, 0 failed` | 跳过项保持原状态，未计为通过 |
| R-14 | Excel 验收状态 | ✅ 完成 | 83 项 V1 为 82 通过、1 不适用；12 项 V1.1 全通过；7 项交付物完成 | 无 |
| R-15 | 验收证据可达性 | ✅ 完成 | 抽取并核对 208 个证据引用，全部可以定位 | 两个被识别为路径的值实际是工作表名，已排除 |
| R-16 | 本地离线与应用日志 | ✅ 完成 | 模型启用 offline；本轮应用日志未发现 `500`、`Traceback` 或异常 | 无 |
| R-17 | 当前 v3 数据备份 | ⚠️ 有条件完成 | `20260911-142347` 备份已生成，数据库和文件归档哈希一致 | 最新备份尚未做独立恢复演练；11:53 备份已有恢复对账 |
| R-18 | QA 记录同步 | ✅ 完成 | 词库 v2 已更正为 v3；回归数更新为 1237；敏感词页面面包屑已修正并用真实 Chrome 复核 | Excel 历史行保留当时结果 |
| R-19 | 源码冻结与可复现版本 | ❌ 未完成 | 当前分支有 113 个已跟踪修改和 113 个未跟踪项 | 正式源码交付前审查差异、排除运行产物并形成可回退提交 |
| R-20 | 非当前模型下载残留 | ⚠️ 有条件完成 | 4B/14B 目录约 11.2GB；51 个 `.part` 文件约 7.0GB；均未加载 | 不影响演示；演示后确认保留策略再清理 |

## 现场演示判断

- ✅ 登录、业务入口、汇报页、数据流程、AI 入口、词库、报告版本、DOCX 下载和三个固定端口具备演示条件。
- ✅ 当前本地生成模型是 Qwen3.5-9B Q8_0，2B 已清理，不存在 2B 与 9B 同时加载。
- ⚠️ 最新备份的内容哈希已验证，但独立恢复验证沿用 11:53 备份的既有证据。
- ❌ 当前代码工作区尚未形成正式冻结提交，因此不能把“现场可演示”表述为“正式源码已交付”。

## 正式交付前关闭顺序

1. 审查当前工作树差异，剔除构建、运行和临时文件，形成可回退的正式提交。
2. 对最新备份做一次独立恢复对账，补齐当前 v3 数据快照的恢复证据。
3. 现场演示结束后，再决定并清理未使用的 4B/14B 下载残留。

## 2026-09-11冷启动补充验证

- ✅ 正常停止8893应用、18081模型和58562数据库后，三个固定端口均停止监听。
- ✅ 从桌面启动器重新启动，PostgreSQL、Qwen3.5-9B和8893应用全部自动恢复。
- ✅ Flask与PostgreSQL重新建立连接，状态为`ESTABLISHED`。
- ✅ 模型接口只返回Qwen3.5-9B；登录后成功回读敏感词库v3和既有科研项目。
