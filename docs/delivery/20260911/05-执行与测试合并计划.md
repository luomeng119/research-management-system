# 科研管理系统：本地执行与测试合并计划

更新时间：2026-09-11。

本文件同时是执行计划和测试计划。需求以[需求确认](01-需求确认.md)为准，结果统一写入[最终交付验收表](07-最终交付验收表.xlsx)。

## 1. 当前状态

V1.1已经在当前Mac本地完成：V1原83项保持，新增12项图文整合验收全部通过。合并统计为95项，其中94项通过、1项按范围不适用，失败、阻塞、部分验证和待执行均为0。登录、科研流程、敏感词、9B文本与图片能力、框选、版本、含图Word、采购、真实Chrome、并发和备份恢复都已真实验证。

固定环境不再改变：系统`8893`、Qwen3.5-9B模型`18081`、PostgreSQL`58562`，登录账号`admin/admin`。

## 2. 已完成目标

使用现有合成课题和合成资料，已在当前Mac真实跑通：

**登录 → 上传两张合成科研图片和一份文本资料 → 敏感词处理 → 9B读取图片 → 人工确认 → 生成并修订报告 → 保存版本 → 导出含图DOCX → 重新登录回读。**

## 3. 执行和测试

| 顺序 | 已执行工作 | 验证结果 | 状态 |
|---|---|---|---|
| 1 | 给现有9B加载匹配的视觉投影文件 | 模型与mmproj哈希通过；仍只有一个9B；端口18081；设置页显示“文本 + 图片（已核验）” | 完成 |
| 2 | 上传包含已知事实、未知项和测试敏感词的合成科研图片 | 19.551秒完成最小图片调用；项目、地点、设备、样本、异常、结论、数值和阈值对照通过 | 完成 |
| 3 | 将两张图片与一份文本资料共同生成报告，保存v2并导出含图DOCX | 93.178秒生成；S1/S2/S3可追溯；敏感原图阻断；清洁图嵌入；四页Word视觉检查通过 | 完成 |
| 4 | 用真实Google Chrome从`admin/admin`登录，执行回归和备份恢复 | 绿灯、v2、代称、62927字节DOCX下载通过；V1.1当轮Python 1233、Chromium 6、控制器20项通过；2026-09-11 14:22最终复核Python 1237通过、179跳过、0失败；恢复对账一致 | 完成 |

执行结果已写入Excel的新“V1.1图文整合”工作表，原83项V1结果保持不变。

## 4. 最终交付标准

以下结果已经同时具备：

1. 9B视觉能力真实可用，模型、端口、内存和绿灯一致。
2. 一张合成科研图片能够识别、修改、确认和追溯。
3. 图片结果与文本资料能够共同生成至少两个报告版本。
4. 含图片、图题、来源和版本号的DOCX已在Microsoft Word中打开。
5. 外部Chrome完整流程、异常路径、全量回归和备份恢复通过。
6. Excel新增工作表12项全部通过，没有失败、阻塞、部分验证或待执行。

当前本地交付状态为完成。现场演示继续使用同一固定端口和合成案例。

## 5. 本次不做

不做热加载、Ollama迁移、FastAPI、前后端重构、向量库、RAG，也不使用甲方数据。

下午现场只演示已经在当前Mac跑通的合成案例，不导入甲方文件，不在现场临时改模型、端口或程序。
