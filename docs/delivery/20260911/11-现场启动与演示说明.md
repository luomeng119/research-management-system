# 当前Mac现场启动与真实案例演示说明

更新时间：2026-09-11 14:30。适用设备为当前M4 Pro、48GB Mac。当前版本已完成当前Mac本地演示交付：真实业务主线、受控AI入口、报告与通用采购DOCX导出、10会话容量、备份和独立恢复均已实际运行。83项V1验收为通过82、不适用1；12项V1.1新增验收全部通过。正式源码冻结状态以《现场演示遗留问题复核》为准。

## 1. 启动和登录

1. 双击本目录的`13-启动科研管理系统.command`。启动器只启动缺少的本任务服务，复用身份匹配的既有服务，不下载模型、不安装依赖、不停止其他应用服务；成功后自动打开外部浏览器。
2. 浏览器未自动打开时访问[科研创新管理](http://127.0.0.1:8893)。本机演示账号为`admin`，密码为`admin`。
3. 页面顶部模型状态只有在健康检查和模型身份均匹配时才显示绿色。当前交付模型应显示`Qwen3.5-9B`。
4. 启动异常时运行以下检查。三个结果均为“正常”才进入演示：

```bash
'/Users/lm/Documents/Codex/tasks/20260901-科研管理系统V1产品化/work/dev-workspace/科研管理系统/.venv/bin/python' '/Users/lm/Documents/Codex/tasks/20260910-科研管理系统复盘与本地模型调整/runtime/offline_runtime.py' check
```

当前端口约定：8893为业务系统，18081为本任务生成模型；18080是其他既有服务，不得停止或改动。

## 2. 本地模型定位

当前使用`Qwen3.5-9B-Q8_0.gguf`，通过llama.cpp本地服务和OpenAI兼容HTTP协议调用；不是OpenAI云服务，DeepSeek已关闭。模型文件SHA256为`809626574d0cb43d4becfa56169980da2bb448f2299270f7be443cb89d0a6ae4`，上下文8192，关闭思考输出，串行推理。

模型只用于四类业务动作：科研提案建议、科研报告资料整合与初稿（其中图片来源先由模型提取）、多方资料补充后的报告修订、阅读报告时的框选文字建议。敏感词自动替换、权限、状态流转、版本保存、引用校验、文件哈希、下载和备份恢复均由确定性代码完成。通用文档AI校对不在当前范围，工具页显示“当前范围未启用”，旧接口直接返回404。

MiniCPM5-2B在真实报告修订中不满足必需任务，已从当前产品配置和本地模型目录移除。当前唯一业务模型为Qwen3.5-9B。

## 3. 现场真实案例和演示顺序

演示课题为“便携式野外试验环境数据采集终端研制”，使用明确标注的合成测试资料，验证软件和模型能力，不冒充甲方真实科研成果。

建议按以下顺序操作：

1. 从登录首界面用`admin/admin`登录，进入工作台，说明六个入口：科研提案、科研项目、专家库、科研资源、辅助工具和系统设置。
2. 打开提案`TP-20260910-25C5B9B0`，展示多方资料、敏感词替换、提案建议和人工采用边界。
3. 打开提案报告`3e15daef-b73b-4801-9df0-e7fd5a8260f4`，当前已保存v2；展示多资料来源、事实引用、版本记录和DOCX导出。
4. 打开项目`KY-20260910-88A63119`，UUID为`67c955bc-eb96-460c-a836-b3a9fa41461d`；展示立项关联、进展、变更、v1问题、v2改进、成果和失败结题记录。
5. 打开项目报告`1d9002cc-2fa8-4002-9bd9-018b9b8c2b5f`，当前已保存v3；展示补充资料修订、历史版本、框选建议、人工修正边界和DOCX导出。
6. 进入模型设置，确认Qwen3.5-9B状态为绿色且身份已核验，说明只允许一个受控生成模型驻留。
7. 展示专家库、设备和设备组、标准法规、科研模板、通用表格、简单财务及票据OCR。通用文档AI校对保持禁用。
8. 进入简单财务的报销单据，打开系统通用科研物资采购申请单，展示多条物品的Web结构化编辑、保存和DOCX下载；下载件可在Microsoft Word中打开。
9. 退出并重新登录，再回到项目和两份报告，证明数据持久保存。

提案报告v2下载件位于`field-terminal/downloads/proposal-report-v2.docx`，SHA256为`333bf8e24ffe30f64c5ecd8f06f53500c4c85d6af2241f1a6a8985bb014d8111`。项目报告v3下载件位于`field-terminal/downloads/project-report-v3.docx`，SHA256为`268a578eae70f4550fb764aad575c3743163935f95f775fd0b9ef860c241b1ec`。

当前系统提供两类Word能力：科研报告的网页纯文本编辑、版本留痕和DOCX导出；通用业务单据的网页结构化字段编辑、多条明细和DOCX生成。当前没有任意外部`.docx`在浏览器内的完整所见即所得或无损往返编辑器；如后续确需该能力，应单独集成OnlyOffice、Collabora或同类Office组件。

## 4. 已验证结果和交付边界

- 主程序最终复核：Python 1237项通过、179项跳过、0失败；领导介绍页固定8893端口Playwright复核1项通过；此前Chromium完整端到端7项通过；`git diff --check`通过。
- 独立业务验收：在新PostgreSQL测试库从提案、论证、立项、启动、进展、成果到结题完整运行，并覆盖三类项目、备份恢复、恢复后多视口回读、外部请求为零以及错误恢复拒绝。
- 通用采购闭环：外部Chrome以`admin`填写两条科研物品，保存后字段保持数组结构；下载DOCX为一页、含两个完整明细块、无列表文本，并在Microsoft Word中实际打开。TL-04通过。
- 当前真实数据保护：最新v3数据备份为`backups/20260911-142347/`，数据库与文件归档哈希已匹配；`backups/20260911-115319/`已恢复到独立数据库和文件目录并完成双重对账。最新14:23备份尚未单独恢复演练。
- QA-03容量与并发已经通过：10个同权会话、200次普通请求p95为0.036秒；10路同版本保存仅1路成功，9路409拒绝；真实9B框选建议11.64秒。QA-06最终汇总门禁通过。

当前版本适合在这台Mac上演示已通过的真实闭环。结论限定于当前Mac、合成科研案例和已确认功能范围；甲方真实课题资料、专用敏感词库和其他操作系统仍需在获得实物后另行验证。

## 5. 备份和故障处理

备份前保存并退出所有编辑，结束上传、导入和AI采纳操作。随后运行：

```bash
'/Users/lm/Documents/Codex/tasks/20260901-科研管理系统V1产品化/work/dev-workspace/科研管理系统/.venv/bin/python' '/Users/lm/Documents/Codex/tasks/20260910-科研管理系统复盘与本地模型调整/runtime/offline_runtime.py' backup
```

每次备份会生成`qa_offline.dump`、`runtime-files.tar.gz`和`manifest.json`。新备份必须单独核验，不能沿用旧备份的通过结论。

排障先读取本目录`app.log`、`model.log`、`postgres.log`及`check`结果。不要反复强杀进程；不要停止18080；不要把环境变量、口令或旧密钥复制进日志和聊天。模型停机时，手工业务功能应继续可用，但提案助手、报告生成修订和框选建议会明确不可用。

## 6. 2026-09-11冷启动补充验证

已依次正常停止8893应用、18081模型和58562数据库，确认三个端口均停止监听；随后执行桌面启动器。脚本自动启动PostgreSQL、加载Qwen3.5-9B并启动8893应用。最终检查结果为数据库、模型和应用均正常，Flask与PostgreSQL连接为`ESTABLISHED`，模型接口只返回一份Qwen3.5-9B。重新登录后能够回读敏感词库v3及既有科研项目，数据未丢失。
