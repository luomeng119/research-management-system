# 领导介绍页阶段回归记录

## 用户纠正与处理

- 用户指出“图片”应为流程图、架构图等讲解图，不是场景插画。
- 已从项目删除三张场景插画，改为七张 HTML/CSS 结构图。
- 介绍页目录先行完成，登录页随后在不改变表单契约的前提下优化。

## 测试驱动记录

1. 先增加公开路由、七目录、离线资源和登录表单契约测试。
2. 首次运行时登录页缺少 `/system-intro` 链接，出现 1 个预期失败。
3. 完成登录页优化及介绍入口后，目标 Python 测试为 `3 passed`。
4. 新增 Playwright 桌面真实渲染测试，逐项点击并检查 1440×900 无横向溢出。

## 已验证契约

- 匿名访问 `/system-intro` 返回 200。
- 业务页面仍受登录保护。
- 七个目录项切换正确，URL hash 可直接打开指定章节。
- 页面零外网请求，无 CDN 或远程图片。
- 登录页用户名、密码和 CSRF 字段保持不变。
- 1440×900 桌面视口无横向溢出；移动端已按用户要求移出范围。

## 最终回归

- `.venv/bin/python -m pytest -q`：`1237 passed, 179 skipped`（2026-09-11 14:22 重新全量执行）。
- `npx playwright test --project=chromium --workers=1`：`7 passed`，移动端用例已删除。
- 本地 Chrome 真实登录：`admin/admin` 成功进入工作台。
- `runtime/offline_runtime.py check`：PostgreSQL、模型、应用均正常。

## 2026-09-11 现场页复测

- 发现并修复：介绍页继承系统基础模板，导致已登录会话中正文被工作台外壳隐藏；介绍页现为独立 HTML。
- 发现并修复：已登录用户点击“进入系统”会落到空白登录外壳；按钮现按会话显示“进入系统”或“返回系统”。
- 修复后 Python 契约测试：`3 passed`；介绍页 Playwright：`1 passed`。
- 本地真实 Chrome 已逐项点击七个目录；登录态介绍页完整显示，“返回系统”成功进入工作台。
- 页面错误、资源加载失败、外网请求和桌面横向溢出均为零。

## 2026-09-11 数据流程与 AI 标识复测

- 新增“数据流程”目录，按资料来源、入库与关联、文档操作、版本与输出四类说明文档来龙去脉。
- 插入、编辑、合并、删除均在数据流程图中列出，删除边界明确为受控移除业务关联。
- 既有 AI 功能统一增加“AI”标识：提案助手、整合报告草稿、选区措辞建议；图片来源处标识由本地模型提取。
- Python 结构与安全契约：`4 passed`；提案助手：`38 passed, 1 skipped`；科研报告：`96 passed`；选区交互：`10 passed`。
- 介绍页 Playwright 直接连接固定地址 `127.0.0.1:8893`：`1 passed`；本地真实 Chrome 已确认七项目录、数据流程图及报告页 AI 按钮可见。
- `runtime/offline_runtime.py check`：PostgreSQL、模型、8893 应用均正常。

## 2026-09-11 敏感词库入口复测

- 词库维护模块原已存在，但入口只位于管理员下拉菜单；现已在“辅助工具”首页增加“敏感词库维护”卡片。
- 介绍页“本地 AI”章节明确维护路径：辅助工具 → 敏感词库维护，并说明原词与代称按版本维护。
- 当前管理员账号实际打开 `/admin/sensitive-terms` 成功；页面显示当前 v3、逐条编辑、启用/禁用、JSON 导入、预览、新版本保存和历史版本入口。
- 词库维护、权限、版本以及介绍页结构测试：`37 passed`；PostgreSQL、模型和 8893 应用检查正常。
- 用户指出逐条编辑区没有明确的新增入口后，已增加“＋ 添加规则”按钮；真实 Chrome 点击后成功新增第 7 行并自动聚焦“原词”输入框。词库服务与页面回归：`32 passed`。
