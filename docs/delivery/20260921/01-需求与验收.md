# 01 需求与验收

- 任务：科研管理系统移除本地模型运行资产、隐藏本地模型功能并建立公开源码协作仓库
- 需求版本：v0.3（GitHub公开免邀请克隆方案已确认）
- 日期：2026-09-21
- 权威源码：`/Users/lm/Documents/Codex/tasks/20260901-科研管理系统V1产品化/work/dev-workspace/科研管理系统`
- 执行路线：`dev-workflow` 改造流程（P0确认前仅允许只读调查与记录同步）

## 已确认的用户意图

1. 甲方暂时不采用本地模型方案。
2. 删除本机为本项目下载的本地模型资产，释放空间。
3. 产品界面隐藏本地模型相关功能，但保留代码和扩展接口，便于以后重新启用。
4. 使用 GitHub 账号 `luomeng119` 新建公开仓库，供同事无需邀请即可克隆和二次开发。
5. 公开访问只解决读取与克隆；推送代码仍需个人 GitHub 身份以及仓库写权限，不共享账号凭据。
6. 当前项目从需求、设计、开发进展、验证到交付的相关文档必须随源码同步到仓库。
7. 模型之外的下载残片也应清理，但只删除已确认属于本项目模型下载的残片。

## 当前观察事实

- 当前源码已整理为 `main` 分支并推送到公开仓库 `luomeng119/research-management-system`。
- 本项目原本约 20.9 GiB 的本地生成模型、多模态投影文件和未完成下载分片已删除；项目范围复查无模型或下载残片。
- `18081` 本地生成模型服务不再属于当前交付；`18080` 的 BGE-M3 嵌入服务属于其他用途，删除前后哈希一致。
- 应用默认 `AI_FEATURES_VISIBLE=False`、`AI_PROVIDER=DISABLED`；真实浏览器复核确认领导介绍、辅助工具、提案和报告页面不显示本地模型入口，AI 专用路由返回 404。
- 已在固定地址 `http://127.0.0.1:8893` 和隔离 PostgreSQL 验收库中完成登录→提案→报告 v1/v2→DOCX导出→敏感词库 v1 的真实浏览器链路；验收后临时应用与数据库已停止。
- `luomeng119` 已通过 `gh` 登录；`research-management-system` 已创建为公开仓库。
- 已存在的 `ai-archives-research-platform` 是另一个“AI档案编研智能平台”仓库，不得覆盖或复用。
- GitHub 个人账号私有仓库只区分仓库所有者和协作者；协作者获得读写权限，邀请时必须提供 GitHub 用户名。

## 拟确认的交付结果

1. 删除项目模型目录中的模型文件和下载残片，保留删除清单和释放空间证据。
2. 新增默认关闭的产品级 AI 可见性开关；默认情况下隐藏模型设置、状态、提案助手、报告生成/图片提取/选区建议及领导介绍页中的模型展示；相关后端能力保持可恢复。
3. 保留科研报告的人工创建、编辑、版本、敏感词规则和 DOCX 导出，不因隐藏 AI 而删除业务能力。
4. 更新启动说明和 README，使默认启动不再依赖本地模型或 `18081`。
5. 加固 `.gitignore`，排除模型、运行数据、上传文件、日志、会话、密钥和本机证据；执行当前工作树与 Git 历史的敏感信息检查。
6. 将清理后的当前产品源码提交并推送到新的公开仓库 `luomeng119/research-management-system`，默认分支为 `main`。
7. GitHub访问方案已经确定为公开仓库：无需邀请即可克隆；推送仍需个人 GitHub 身份和写权限，不共享账号凭据。
8. 把桌面最终交付目录中与科研管理系统直接相关的 00—13 号文件，以及本轮清理/隐藏/仓库交付记录，整理到仓库 `docs/delivery/`；14—25 号 dev-workflow 技能复盘资料不混入产品仓库。

## 范围与排除

- 不删除本地 AI 实现源码、数据库迁移或历史测试；仅默认隐藏并停止加载。
- 不删除 `/Users/lm/.local/models/bge-m3-q8_0.gguf` 或其他项目使用的嵌入模型。
- 不改用 DeepSeek、OpenAI 或其他云端模型。
- 不覆盖现有 `ai-archives-research-platform`；新仓库固定为公开可见。
- 不上传数据库、真实业务数据、会话、日志、模型文件、构建目录或本机验收证据。
- 本次只完成本机代码与 GitHub 源码交付，不部署到甲方环境。

## 可观察验收标准

| ID | 验收标准 | 证据 |
| --- | --- | --- |
| AC-01 | 项目模型目录删除，至少释放约 20 GiB，项目范围内不再存在 GGUF 或下载分片 | 删除前后清单、`du`/`find` 结果 |
| AC-02 | 默认配置下所有本地模型入口、按钮和介绍文案不可见；相关直接路由返回 404 或不可用 | 模板/路由测试、真实浏览器检查 |
| AC-03 | 人工科研报告、版本、敏感词和 DOCX 导出仍可用；核心回归通过 | 定向测试与全量回归结果 |
| AC-04 | 公开仓库创建成功，默认分支 `main`，清理后的源码可克隆且不含模型、运行数据和已知凭据 | GitHub API、重新克隆、秘密扫描、哈希/提交号 |
| AC-05 | 未登录用户可匿名克隆；推送仍要求个人 GitHub 身份和写权限 | GitHub API、禁用凭据助手的匿名克隆 |
| AC-06 | 需求、设计、执行测试、验收、启动和样例交付文档随源码存在，README有文档入口 | 仓库文档清单、链接检查、重新克隆回读 |

## 尚待用户确认

- 无。仓库名确定为 `research-management-system`，可见性确定为公开。
