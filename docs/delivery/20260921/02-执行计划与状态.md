# 02 执行计划与状态

- 状态：本地改造已获授权；GitHub可见性/权限决策待定
- 当前允许：模型资产清理、功能隐藏、文档归档、测试与仓库安全准备
- 当前禁止：在GitHub创建仓库、改变可见性、推送或设置协作者，直至“私有”与“免邀请”冲突解决
- Native Goal：`active`；已通过 `create_goal` 创建并由 `get_goal` 回读确认；threadId `01a08932-cf5e-72b0-a06d-4dc12f7c18ee`

## 指令与工作流版本

| 类型 | 路径 | SHA-256 / 版本 |
| --- | --- | --- |
| 全局指令 | `/Users/lm/.codex/AGENTS.md` | `70e83a785f1fd836ae0e6340ecf755dd58ed32a4004fa963a050e7f9b6e32977` |
| 工作区指令 | `/Users/lm/Documents/Codex/AGENTS.md` | 不存在 |
| 选定工作流 | `/Users/lm/.agents/skills/dev-workflow/SKILL.md` | `9.6.1`；SHA-256 `58f4e2a123b525270b3f509ada40cb799da1d887fd34159ed8a67c007a7607c1`|
| 协调规则 | `/Users/lm/.agents/skills/orchestration/SKILL.md` | SHA-256 `038549bf9cc68801c3675e0723f988e79ae51657868d3e48f23950ca7e3419d9`；Orca `1.4.204`，runtime ready |

## 三记录映射

- 需求与验收：本文件同目录 `01_需求与验收.md`
- 执行计划与状态：本文件
- 结果与验收：本文件同目录 `03_结果与验收.md`
- dev-workflow run：需求确认后在权威源码 `.dev-workflow/runs/` 新建 refactor run

## 计划工作包

| 包 | 动作 | 依赖 | 验收 |
| --- | --- | --- | --- |
| P1 资产清理 | **已完成**：删除整个项目模型目录并复核无 GGUF/下载分片；受保护 BGE-M3 哈希前后一致 | 需求确认 | AC-01 |
| P2 功能隐藏 | **已完成**：Orca `task_cbf1bfdd4d10` 新增默认关闭的 AI 可见性总门禁，完成服务、路由、导航和页面隐藏并保留人工业务 | 需求确认 | AC-02、AC-03 |
| P3 仓库发布准备 | **本地完成**：忽略规则、README、交付说明、当前树/历史秘密扫描和发布集合复核均通过；fresh clone 等待P4远端完成后执行 | P2 | AC-04 |
| P3A 文档同步 | **已完成本地整理**：Orca `task_da63cb038f61` 已复制产品相关00—13号文件，新增当前状态说明和仓库卫生报告，14—25号未入库 | P1、P2 | AC-06 |
| P4 私有仓库交付 | **本地提交完成、外部待决策**：当前分支已整理为 `main`，功能与交付文档均已提交；创建远端、推送和 fresh clone 等待可见性选择，最终 SHA 以远端推送回读为准 | P3 | AC-04 |
| P5 协作权限 | 按最终选择验证匿名克隆或邀请协作者并回读写权限 | 用户决策、P4 | AC-05 |
| P6 独立验证 | **本地完成**：定向/全量测试、文档边界、发布集合、残片忽略、秘密扫描和真实浏览器端到端业务链均通过；fresh clone/权限在P4/P5后执行 | P1-P5 | AC-01 至 AC-06 |

## 临时执行路线判断

需求一经确认，计划具有“产品实现”和“仓库安全发布/独立验证”两个以上独立可验收包，按全局规则暂定使用 Orca `orchestration`；实际启动前重新根据确认后的结构计算一次，不以工具名称代替结构判断。

## 计划覆盖检查

- AC-01 → P1 + P6：已覆盖。
- AC-02 → P2 + P6：已覆盖。
- AC-03 → P2 + P6：已覆盖。
- AC-04 → P3 + P4 + P6：已覆盖。
- AC-05 → P5 + P6：动作已覆盖，但缺少协作者 GitHub 用户名。
- AC-06 → P3A + P6：已覆盖。
- 依赖和下一步：先执行P1、P2、P3和P3A；GitHub外部写入等待用户在“私有逐人邀请”和“公开免邀请拉取”之间选择。
- 结论：本地工作覆盖检查通过；P4/P5因GitHub权限模型冲突暂停，不阻塞P1/P2/P3/P3A/P6的本地部分。

## 当前执行证据

- dev-workflow run：`.dev-workflow/runs/20260921-remove-local-model-publish-source/run.json`，已完成阶段1、2、3、3.5，阶段4执行中。
- Orca run：`run_cd3980550c44`；两个互斥写边界任务已受监督启动。
- P1 删除证据：`evidence/model-assets-deletion.txt`；删除 64 个文件、逻辑大小 22,483,221,919 字节，项目范围剩余模型/下载分片为 0。
- P2 定向验证：默认隐藏与人工业务组合测试 127 passed；更广 AI 显式启用回归 155 passed、1 skipped；均 exit 0。
- P3A 文档验证：历史00—13共14个文件逐字节一致，XLSX/DOCX包完整，21个本地链接通过，12类排除样例和4类保留样例符合规则。
- P6 全量回归：首次发现2个旧断言仍要求AI路由403/介绍页含本地AI专章；按当前交付契约更新断言后，聚焦2项通过，全量 `pytest -q` 为1260 passed、179 skipped、exit 0。
- P6 独立复核：162项AI门禁与人工报告/敏感词/DOCX聚焦测试通过；发布集合550个路径；00—13为14个、14—25为0个、20260921现行记录为5个；当前树532个文本文件和历史929个文本blob的高置信秘密扫描均为0。
- P6 真实浏览器：固定使用 `127.0.0.1:8893`，`admin/admin` 登录成功；真实新建科研提案、报告 v1/v2、DOCX导出和敏感词库 v1；5个AI专用入口均返回404，详见 `evidence/browser-acceptance-summary.md`。
