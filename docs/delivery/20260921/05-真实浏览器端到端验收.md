# 真实浏览器端到端验收

- 日期：2026-09-21
- 固定演示地址：`http://127.0.0.1:8893`
- 环境：本机应用 + 隔离 PostgreSQL 验收数据库；验收结束后服务与临时数据库均已停止
- 范围：登录、提案、人工科研报告、版本、DOCX、敏感词库、AI 隐藏

## 业务链结果

| 步骤 | 结果 |
| --- | --- |
| `admin/admin` 登录 | 通过，进入工作台 |
| 新建科研提案 | 通过，形成《复杂场景便携式光谱检测终端研制》 |
| 从提案进入科研报告 | 通过，关联对象正确 |
| 新建报告 v1 | 通过，保存正文、用途和修改说明 |
| 保存报告 v2 | 通过，v1/v2 历史同时可回看 |
| 导出 v2 DOCX | 通过，HTTP 200，Office DOCX MIME，37,257 字节 |
| 敏感词库“＋ 添加规则” | 通过，增加 `XX研究所` → `X所` |
| 敏感词预览与保存 | 通过，保存为词库 v1，历史版本可回看 |

## AI 隐藏结果

系统介绍、辅助工具、提案、科研报告和敏感词维护页面均未出现 `Qwen`、`本地模型`、`AI 生成`、`模型配置`、`模型状态` 或 `智能助手`。系统介绍页无本地 AI 专章。

以下 AI 专用入口均返回 404：

- `GET /utils/model_config`
- `GET /api/llm/status`
- `GET /research-reports/demo/drafts/demo`
- `GET /research-reports/demo/versions/1/model-draft`
- `POST /api/proposals/demo/assistant-drafts`

## 证据边界

浏览器原始 JSON 与截图保留在本机任务证据目录，不进入源码仓库。本文件只记录脱敏、可复核的验收结论；不把临时数据库、会话、账号哈希或本机运行日志作为交付内容。
