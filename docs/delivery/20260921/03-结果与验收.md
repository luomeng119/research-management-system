# 03 结果与验收

- 当前状态：模型资产清理、功能隐藏、文档同步和本地独立验证完成；GitHub发布与权限验证待完成
- 完成等级：执行中；GitHub权限决策待定

## 已完成的只读核查

1. 确认权威源码、当前分支、未提交改动和无远端状态。
2. 定位本项目模型资产约 20.9 GiB；确认 `18081` 当前未监听。
3. 确认 `18080` BGE-M3 与本次项目生成模型无关，列入保护范围。
4. 确认 GitHub 账号 `luomeng119` 已登录且具备 `repo` 权限。
5. 确认拟用仓库名 `research-management-system` 当前不存在。
6. 确认现有 `ai-archives-research-platform` 是不同项目，不覆盖。
7. 确认模型功能代码已具备部分 `AI_PROVIDER=DISABLED` 保护，但仍有页面文案和按钮需要统一隐藏。

## 验收状态

| ID | 状态 | 说明 |
| --- | --- | --- |
| AC-01 | 通过 | 已删除项目模型目录 64 个文件、22,483,221,919 字节；项目范围 GGUF/下载分片为0；受保护 BGE-M3 哈希前后一致 |
| AC-02 | 通过 | 默认 `AI_FEATURES_VISIBLE=False`；AI服务不挂载，专用路由404，相关页面不显示模型/AI控件；真实浏览器与独立复核通过 |
| AC-03 | 通过 | 人工报告、版本、敏感词、DOCX路径保留；真实浏览器完成提案→报告 v1/v2→DOCX导出和敏感词库 v1；定向组合127 passed，更广回归155 passed、1 skipped；全量1260 passed、179 skipped；独立162项通过 |
| AC-04 | 部分通过 | 本地 `main` 已形成功能与文档齐全的发布集合；远端创建、推送、最终 SHA 回读和 fresh clone 待权限决策 |
| AC-05 | 阻塞决策 | 私有免邀请不可实现，等待选择私有逐人邀请或公开免邀请克隆 |
| AC-06 | 通过（待fresh clone复核） | 产品历史00—13已入 `docs/delivery/20260911`，14—25未入库；20260921现行记录5个，README有入口；本地链接与独立发布集合复核通过 |

## 当前阻塞

- GitHub个人私有仓库无法同时实现“无需邀请即可访问”；外部仓库写入等待用户选择。
- 本地清理、代码隐藏、文档同步和验证不受该冲突影响，继续执行。

## 已完成的资产清理

1. 删除 `/Users/lm/Documents/Codex/tasks/20260910-科研管理系统复盘与本地模型调整/models` 整个目录。
2. 清理范围包含 9B、4B、多模态投影文件、14B 未完成分片和重复下载片段，共释放逻辑大小约 20.94 GiB。
3. 对项目调整目录与权威源码目录再次扫描，未发现 `.gguf`、`.part`、`.partial`、`.aria2`、`.crdownload`、`.segment-*`、`.safetensors` 或超过 500 MiB 的残留下载文件。
4. `/Users/lm/.local/models/bge-m3-q8_0.gguf` 删除前后 SHA-256 一致，未受影响。

## 已完成的功能隐藏与文档同步

1. 新增默认关闭的 `AI_FEATURES_VISIBLE` 产品总门禁；关闭时不加载助手/报告生成服务，AI专用路由返回404。
2. 登录、领导介绍、辅助工具、提案和科研报告页面默认不显示本地模型、Qwen或AI生成动作。
3. 明确保留人工科研报告创建/编辑/版本、敏感词维护和DOCX导出，相关定向与显式启用回归均通过。
4. `docs/delivery/20260911` 保存00—13号产品历史文档，`docs/delivery/20260921`说明当前无本地生成模型交付状态；README、忽略规则与仓库卫生报告已经更新。
5. 全量回归首次暴露2个旧测试断言与新边界不一致；修正为“AI路由404、介绍页无本地AI专章”后，1260项通过、179项按环境条件跳过，无失败。

## 真实浏览器端到端结果

1. 在隔离 PostgreSQL 验收环境中以 `admin/admin` 登录，登录页、工作台与一级导航正常。
2. 新建真实科研提案《复杂场景便携式光谱检测终端研制》，再从提案详情进入科研报告。
3. 创建报告 v1，修订并保存 v2；两个版本均可回看。v2 DOCX 导出返回 HTTP 200、正确 Office MIME 类型和 37,257 字节文件。
4. 敏感词库“＋ 添加规则”按钮可用，成功建立 `XX研究所` → `X所` 规则并保存为 v1，历史版本可回看。
5. 系统介绍、辅助工具、提案、报告和敏感词页均无本地模型/Qwen/AI生成可见入口；5个AI专用路由均返回404。
6. 原始浏览器 JSON 和截图保留于本机任务证据目录，不进入源码仓库；脱敏摘要随源码交付。
