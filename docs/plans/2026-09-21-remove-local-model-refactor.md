# 科研管理系统去本地模型与源码交付实施计划

## 完成目标

在保留现有业务改动的前提下，将本地生成模型从当前交付运行面移除，保留人工科研报告业务与未来扩展代码，整理完整开发进展文档，并将经过安全检查的源码发布为可协作仓库。

## 增量与回滚点

| 顺序 | 增量 | 所有权 | 完成检查 | 回滚点 |
| --- | --- | --- | --- | --- |
| 1 | 新增 `AI_FEATURES_VISIBLE=False` 总门禁并关闭 AI 服务初始化 | 应用代码包 | 配置、应用工厂、路由和模板定向测试 | 恢复门禁修改；不影响模型文件 |
| 2 | 隐藏登录、领导介绍、辅助工具、提案和报告中的 AI 入口 | 应用代码包 | 页面无 AI/模型标识，AI URL 404 | 恢复条件模板；人工业务不变 |
| 3 | 删除精确项目模型目录及下载残片 | 协调者 | 删除后目录不存在、无项目 GGUF/分片，BGE-M3 保留 | 不可逆资产清理；删除前清单为证据，模型可按官方来源重新获取 |
| 4 | 加固 `.gitignore`、整理 README 与 00—13 号产品交付文档 | 文档与仓库包 | 文档清单、链接、状态说明和排除规则通过 | 从 Git 工作树撤销该包，不影响业务代码 |
| 5 | 运行定向、全量、浏览器、文档、secret 和仓库体积检查 | 独立验证包 | AC-01—AC-06 逐项证据 | 发现问题返回责任包修复 |
| 6 | 创建 GitHub 仓库、推送 `main`、重新克隆并验证权限 | 协调者 | API、匿名/授权克隆、提交哈希、内容扫描 | 删除新建仓库或调整可见性；不覆盖其他仓库 |

## 验收矩阵

| 验收项 | 实施 | 验证方法 |
| --- | --- | --- |
| AC-01 | 增量3 | 删除前后 JSON、`find`、磁盘大小、保护路径 |
| AC-02 | 增量1—2 | pytest 模板/路由测试和真实浏览器页面 |
| AC-03 | 增量1—2 | 报告、版本、敏感词、DOCX 定向测试与全量回归 |
| AC-04 | 增量4—6 | GitHub API、fresh clone、模型/数据/凭据扫描 |
| AC-05 | 增量6 | 私有协作者权限回读或公开匿名克隆 |
| AC-06 | 增量4—6 | 文档清单、README 链接、fresh clone 回读 |

## 风险验证

- 先验证门禁默认值，再改模板，防止仅隐藏页面。
- 删除模型前后检查精确路径，并单独确认 `/Users/lm/.local/models/bge-m3-q8_0.gguf` 仍存在。
- 用 Git 跟踪清单和秘密扫描判断发布内容，不能只依赖 `.gitignore`。
- 全量回归使用固定本地应用端口和临时数据库，不引入新模型或云端依赖。
- GitHub可见性决策只阻塞最后的外部发布，不阻塞本地实现与验证。

## AI 评估边界

本轮 AI 能力不属于当前交付，不运行模型质量评测。验证对象是“默认不展示、不加载、不可调用”以及人工业务不受影响。
